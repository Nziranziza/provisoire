---
import { CATEGORY_SLUGS } from '../../../../../../lib/quiz';

export function getStaticPaths() {
  return Object.values(CATEGORY_SLUGS).map((category) => ({
    params: { page: '1', category },
  }));
}

return Astro.redirect(`/en/${Astro.params.category ?? 'traffic-rules'}`, 301);
---
