---
import { CATEGORY_SLUGS } from '../../../../lib/quiz';

export function getStaticPaths() {
  return Object.values(CATEGORY_SLUGS).map((category) => ({
    params: { category },
  }));
}

return Astro.redirect(`/fr/${Astro.params.category ?? 'traffic-rules'}`, 301);
---
